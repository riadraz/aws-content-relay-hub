name: Zenn JP Only Sync

on:
  push:
    branches: [ "main" ]
    paths:
      - "articles/*-ja.md"

jobs:
  zenn-sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log JP article updates
        run: echo "Japanese article updated. Zenn will auto-sync via GitHub連携."
